<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_main"
    app:startDestination="@id/crop_frag">
    <fragment
        android:id="@+id/home_dest"
        android:name="org.wordpress.android.imageeditor.fragments.MainImageFragment"
        android:label="@string/home"
        tools:layout="@layout/main_image_fragment">
    </fragment>
    <fragment
        android:id="@+id/crop_frag"
        android:name="com.yalantis.ucrop.UCropFragment"
        android:label="@string/crop"
        tools:layout="@layout/main_image_fragment">
    </fragment>
</navigation>
